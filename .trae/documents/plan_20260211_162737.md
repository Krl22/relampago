Perfecto. Vamos a implementar la gestión de **Inventario Global (`/stock`)** para el Administrador.

Actualmente ya existe una referencia en el menú lateral para `/stock`, pero la página probablemente no existe o no está implementada completamente. También existe `MyStock` (para clientes), pero tú necesitas una vista global para Admin.

### Requerimientos:
1.  **Vista Global**: Ver el stock de **todas** las empresas en una sola tabla.
2.  **Filtros**: Poder filtrar por nombre de empresa o producto.
3.  **Gestión**: Poder agregar, editar o eliminar stock en nombre de cualquier empresa.

### Plan de Implementación:
1.  **Crear `src/pages/Stock.tsx`**:
    *   Similar a `Companies.tsx` pero enfocado en la tabla `stock`.
    *   Debe hacer un JOIN con `companies` para mostrar a quién pertenece cada producto.
    *   Botón "Nuevo Producto": Debe permitir seleccionar primero la empresa y luego llenar los datos del producto.
    *   Botón "Editar": Modificar cantidad o detalles.

2.  **Actualizar Routing (`App.tsx`)**:
    *   Asegurar que la ruta `/stock` apunte a este nuevo componente y esté protegida (solo Admin/Staff).

3.  **Ajustes de Base de Datos**:
    *   Verificar que la tabla `stock` tenga las políticas RLS adecuadas para que el Admin pueda leer y escribir todo.

Empezaré creando la página de Inventario Global.