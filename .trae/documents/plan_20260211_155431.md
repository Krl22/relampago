Entendido. Vamos a limpiar y mejorar la navegación.

### Cambios Solicitados:
1.  **Eliminar "Nuevo Pedido" interno**:
    *   Quitar la ruta `/orders/new` y el botón "Nuevo Pedido" en la página de lista de pedidos.
    *   Eliminar el archivo `NewOrder.tsx` ya que no se usará.
    *   *Nota*: Si en el futuro quieres crear pedidos manualmente, usarás el link público, que ya está accesible desde el Dashboard.

2.  **Agregar Gestión de Compañías ("Empresas")**:
    *   Crear una nueva sección en el Sidebar: "Empresas" (visible para Admin/Staff).
    *   Crear página `src/pages/Companies.tsx`:
        *   Tabla con lista de empresas.
        *   Posibilidad de Editar (Nombre, RUC, Dirección, Distrito).
        *   (Opcional) Botón para crear nueva empresa manualmente si se requiere.

### Plan de Implementación:
1.  **Sidebar**: Modificar `AppLayout.tsx` para quitar "Nuevo Pedido" y agregar "Empresas".
2.  **Routing**: Modificar `App.tsx` para quitar la ruta antigua y agregar la nueva `/companies`.
3.  **UI Lista Pedidos**: Modificar `Orders.tsx` para quitar el botón "Nuevo Pedido".
4.  **Nueva Página**: Crear `src/pages/Companies.tsx` con una tabla editable simple (usando Shadcn UI Dialog para editar/crear).

Empezaré creando la nueva página de gestión de empresas.