¡Entendido! Agregaré un filtro por empresa en la página de Stock Global. Esto es muy útil para que puedas ver rápidamente el inventario de un cliente específico sin tener que usar el buscador general.

### Plan de Implementación:

1.  **Modificar `Stock.tsx`**:
    *   Agregar un estado `selectedCompany` para almacenar el filtro actual.
    *   Agregar un selector (`<select>`) en la cabecera de la tarjeta, junto al buscador.
    *   Este selector se llenará con la lista de `companies` que ya estamos cargando.
    *   Actualizar la lógica de filtrado `filteredStock` para considerar también la empresa seleccionada.

### Cambios en UI:
*   Añadiré un dropdown que diga "Todas las empresas" por defecto.
*   Al seleccionar una empresa, la tabla solo mostrará los productos de esa empresa.

¡Procedo con el cambio!