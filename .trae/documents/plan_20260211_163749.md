Entendido. Quieres agregar un campo de **número de contacto (teléfono)** a la tabla de Empresas (`companies`), para tener el contacto del cliente a la mano.

### Plan de Implementación:
1.  **Base de Datos (SQL)**:
    *   Generar un script SQL para agregar la columna `phone` (texto) a la tabla `companies`.

2.  **Frontend (`Companies.tsx`)**:
    *   Actualizar la **Tabla**: Agregar una columna "Teléfono".
    *   Actualizar el **Formulario (Dialog)**: Agregar un input para editar/crear el teléfono.
    *   Actualizar el esquema de validación (`zod`) y los tipos.

3.  **Actualizar Tipos Globales (`database.ts`)**:
    *   Reflejar el nuevo campo `phone` en la definición de tipos de TypeScript.

Procedo con los cambios. Empezaré creando el script SQL para que puedas ejecutarlo en Supabase.